import React from 'react';
import ReactDOM from 'react-dom';
import NowPlayingCard from './Card.jsx';
import Editor from './Editor.jsx';

ReactDOM.render(
  <Editor />,
  document.getElementById('root'),
);
